\documentclass[a4paper, 12pt, twoside]{report}
\usepackage[utf8]{inputenc}		% LaTeX, comprend les accents !
\usepackage[T1]{fontenc}		

\usepackage[french]{babel}
\usepackage{lmodern}

\usepackage[top=2.5cm, bottom=2cm,
			left=3cm, right=2.5cm,
			headheight=15pt]{geometry}
\usepackage{graphicx}
\usepackage{eso-pic}	% Nécessaire pour mettre des images en arrière plan
\usepackage{array}
\usepackage{hyperref}
\usepackage{lastpage}
\usepackage{colortbl}
\usepackage[table]{xcolor}
\usepackage{float}
\definecolor{bleuleger}{RGB}{0,0,200}


\usepackage{listings}
\usepackage{amsopn}
\usepackage{color}
\lstnewenvironment{codeC}[1][]
{%
	\lstset{language=C,
		frame=single,
		captionpos=b,
		backgroundcolor=\color{bleuleger!5},
		basicstyle=\ttfamily\tiny,
		numbers=left,
		numberstyle=\color{black},
		numbersep=5pt,
		breaklines=true,
		tabsize=4,
		keywordstyle=\bfseries\color{green!40!black},
		stringstyle=\color{red}\ttfamily,
		identifierstyle=\color{blue},
		caption={[#1]{#1}},
		commentstyle=\color{purple!40!black}}
}
{}
\input{pagedegarde}


\author{Kenza MENAD, Lyna BAOUCHE, Dyhia SELLAH}
\title{Optimisation des tournées en entrepôt : Hybridation du problème du Voyageur de
Commerce (TSP) et du Clustering}
%\entreprise{Le nom de votre entreprise}
%\fonction{Votre fonction}
\datedebut{Mars 2026}
\datefin{Avril 2026}


 %\jurya{Mme. Marta Rukoz}{Professeur des universités}{Responsable du master}

%\juryb{Mme. Prénom Nom}{Titre de votre tuteur enseignant}{Tuteur enseignant}
%Le titre de votre enseignant référent est soit Maître de conférences, soit Professeur des universités. Si vous aves un doute, demandez moi.

%\juryc{M. Prénom Nom}{Poste de votre maître d'apprentissage}{Maître d'apprentissage}
%Demandez à votre maître de stage quel est son poste. Par exemple, directeur du système d'information, chef de projet, responsable d'équipe etc...

%\juryd{M. Prénom Nom}{poste de votre maître de stage}{Maître de stage}
%Ajouter le juryd si votre maître de stage viens accompagné d'un collègue.




%\usepackage[firstpage]{draftwatermark}
%\usepackage{tcolorbox}
%\SetWatermarkText{Confidentiel}
%\SetWatermarkScale{0.9}
%\SetWatermarkColor{red!20}
\begin{document}
\pagedegarde
\tableofcontents

\chapter{Introduction}
La préparation de commandes, ou Order Picking, est l'une des
opérations les plus coûteuses dans la gestion d'un entrepôt. Elle représente
en moyenne 55\% des coûts opérationnels d'un entrepôt, et consiste à collecter
des articles dispersés dans différentes zones pour constituer des commandes
clients. Optimiser les déplacements du préparateur permet de réduire
significativement ces coûts et d'améliorer la productivité.\\

Ce problème peut être modélisé comme un \textbf{Problème du Voyageur de
Commerce} (TSP — \textit{Travelling Salesman Problem}) : le préparateur
doit visiter un ensemble de points (les articles à collecter) en parcourant
la distance la plus courte possible, en partant et en revenant au dépôt.
Cependant, lorsque le nombre d'articles est élevé (environ 119 articles
dans notre cas), l'espace de recherche explose il y a $119!$ tournées
possibles ce qui rend une résolution exacte impossible en temps raisonnable.\\

Pour faire face à cette complexité, nous proposons une \textbf{approche hybride}
combinant deux familles de méthodes complémentaires :\\

\begin{itemize}
    \item Le \textbf{Machine Learning} (clustering K-Means) pour décomposer
    intelligemment le problème en sous-problèmes plus petits, en regroupant
    les articles géographiquement proches en batches cohérents.

    \item Les \textbf{Métaheuristiques} (Algorithme des Colonies de Fourmis
    et Algorithme Génétique) pour optimiser les tournées à l'intérieur de
    chaque batch, et comparer leurs performances.\\
\end{itemize}

L'idée centrale est d'utiliser le clustering en amont de la
métaheuristique : plutôt que de résoudre un TSP global de 119 points,
on résout 4 sous-TSP d'environ 30 articles chacun, ce qui réduit
drastiquement la complexité du problème.
% =========================================================
% CHAPITRE 2 : MODÉLISATION ET STRATÉGIES DE RÉFÉRENCE
% =========================================================
\chapter{Modélisation et Stratégies de Référence}

\section{Modélisation de l'environnement (Warehouse Environment)}
La première étape de notre travail a consisté à créer une simulation fidèle des contraintes physiques d'un entrepôt. Nous avons modélisé cet espace sous la forme d'une grille orthogonale de 200 × 200 unités.

\subsection{Distance de Manhattan}
Contrairement à une distance à vol d'oiseau (Euclidienne), les préparateurs en entrepôt se déplacent le long d'allées perpendiculaires. Nous avons donc implémenté la \textbf{distance de Manhattan} ($L_1$ norm) pour calculer tous les déplacements :
\begin{equation}
    d(A, B) = |x_A - x_B| + |y_A - y_B|
\end{equation}
Cette modélisation garantit que nos résultats d'optimisation sont transposables à une configuration réelle de rayonnages. Le dépôt, point de départ et d'arrivée de chaque tournée, a été fixé aux coordonnées $(0,0)$.

\begin{figure}[H]
    \centering
    % Remplace par le nom de l'image générée par ton script warehouse_env.py
    \includegraphics[width=0.7\linewidth]{../../env/fig_warehouse_plan.png}
    \caption{Visualisation de l'environnement de l'entrepôt avec le dépôt et une vague de commandes générée aléatoirement.}
    \label{fig:warehouse_plan}
\end{figure}

\section{Algorithme de Référence : Le Plus Proche Voisin (Baseline)}
Afin d'évaluer l'efficacité de nos métaheuristiques, nous avons implémenté un algorithme "Glouton" (Nearest Neighbor).

\subsection{Principe et Évaluation}
À chaque étape, le préparateur se rend à l'article le plus proche de sa position actuelle. Cet algorithme nous sert de ligne de base (Baseline). Comme le montre le tableau \ref{tab:baseline_eval}, sa complexité algorithmique très faible lui permet de s'exécuter de manière quasi-instantanée, même sur de gros volumes.

\begin{table}[H]
\centering
\begin{tabular}{|c|c|c|}
\hline
\rowcolor{gray!30}
\textbf{Taille de la commande (Articles)} & \textbf{Distance Totale (Unités)} & \textbf{Temps d'exécution (s)} \\
\hline
10 & 284 & 0,00012 \\
\hline
30 & 642 & 0,00045 \\
\hline
50 & 918 & 0,00103 \\
\hline
100 & 1340 & 0,00350 \\
\hline
\end{tabular}
\caption{Évaluation des performances de la Baseline selon la taille du problème.}
\label{tab:baseline_eval}
\end{table}

\textbf{Limites :} Bien qu'extrêmement rapide, cet algorithme manque de vision globale. Il a tendance à "s'enfermer" aux extrémités de l'entrepôt, générant un long et coûteux trajet de retour vers le dépôt.

\section{Optimisation par Algorithme Génétique (AG)}
En complément des colonies de fourmis, nous avons développé un \textbf{Algorithme Génétique} (AG) pour optimiser les tournées locales.

\subsection{Paramétrage et Convergence}
L'algorithme simule l'évolution naturelle (population de 100 individus, 300 générations, taux de mutation de 5 \%). Les opérateurs génétiques utilisés sont la sélection par tournoi, le croisement de type \textit{Order Crossover} (OX) adapté au TSP, et la mutation par échange (\textit{Swap}).

La figure \ref{fig:convergence_ag} illustre la courbe de convergence de notre Algorithme Génétique face à la Baseline sur une commande de taille moyenne.

\begin{figure}[H]
    \centering
    % Remplace par le nom de l'image générée par la fonction plot_route/history dans genetic_algo.py
    \includegraphics[width=0.8\linewidth]{../../metaheuristics/fig_convergence_ag.png}
    \caption{Courbe de convergence de l'Algorithme Génétique par rapport au score Glouton.}
    \label{fig:convergence_ag}
\end{figure}

\section{Analyse de Mise à l'Échelle (Scaling Analysis)}
Pour justifier l'intervention du Machine Learning (K-Means) dans notre pipeline, nous avons mené une expérience de résistance à l'échelle de notre Algorithme Génétique, détaillée dans le tableau \ref{tab:scaling_analysis}.

\begin{table}[H]
\centering
\begin{tabular}{|l|c|c|c|}
\hline
\rowcolor{gray!40}
\textbf{Scénario} & \textbf{Distance Baseline} & \textbf{Distance AG} & \textbf{Gain/Perte (\%)} \\
\hline
\rowcolor{green!15}
Phase 1 : Petite commande (25 arts.) & 512 & 435 & \textbf{+ 15,0 \%} \\
\hline
\rowcolor{red!15}
Phase 2 : Vague complète (130 arts.) & 1680 & 2105 & \textbf{- 25,2 \%} \\
\hline
\end{tabular}
\caption{Comparaison des performances Baseline vs Algorithme Génétique selon le volume.}
\label{tab:scaling_analysis}
\end{table}

\subsection{L'effondrement combinatoire}
Les résultats révèlent un comportement critique : sur un petit volume (25 articles), l'AG surpasse la Baseline en trouvant des raccourcis globaux. Cependant, lorsque nous lui soumettons l'entrepôt complet (130 articles), l'AG s'effondre. L'espace de recherche devient si vaste ($130!$ permutations) que l'algorithme ne parvient plus à converger vers une solution acceptable dans un temps imparti, finissant par faire pire que le Glouton basique.

\begin{figure}[H]
    \centering
    % Remplace par l'image générée par ton script distance_curve_analysis.py
    \includegraphics[width=0.9\linewidth]{../../metaheuristics/fig_scaling_analysis.png}
    \caption{Évolution croisée des performances mettant en évidence l'effondrement de l'AG face à la complexité combinatoire.}
    \label{fig:scaling_analysis}
\end{figure}

\textbf{Conclusion intermédiaire :} Cette analyse démontre formellement qu'une métaheuristique seule ne peut pas absorber la charge d'un entrepôt entier. Cela justifie pleinement notre architecture finale : utiliser le \textbf{Clustering (Machine Learning)} pour découper l'entrepôt en petits lots ($< 40$ articles), afin de toujours maintenir nos métaheuristiques dans leur "zone de victoire" (Phase 1).


\chapter{Intégration du Machine Learning}

Notre problème consiste à minimiser la distance parcourue par un préparateur de commandes dans un entrepôt de 200 × 200 unités, avec 119 articles à collecter. Si on soumet directement ces points à une métaheuristique (ACO ou algorithme génétique), on obtient un TSP de grande taille : l'espace de recherche explose (119! permutations possibles) et la convergence est très lente.

Notre solution : utiliser le Machine Learning en amont pour regrouper les articles géographiquement proches en batches. Chaque batch devient alors un sous-TSP de petite taille, que la métaheuristique résout rapidement.

\section{Les algorithmes de clustering évalués}
Le clustering est une méthode d'apprentissage non supervisé : elle regroupe des données en groupes homogènes (clusters) sans étiquette préalable. Ici, chaque point est la coordonnée (x, y) d'un article. Nous avons comparé quatre algorithmes de familles différentes :
\begin{itemize}
    \item \textbf{K-Means} : algorithme de partitionnement itératif basé sur la
    minimisation de la variance intra-cluster (WCSS). Il assigne chaque point au
    centroïde le plus proche, puis recalcule les centroïdes, et répète jusqu'à
    convergence.

    \item \textbf{Agglomerative Clustering} : algorithme hiérarchique ascendant.
    Il commence avec chaque point dans son propre cluster, puis fusionne
    progressivement les clusters les plus proches selon un critère de liaison
    (ici : Ward).

    \item \textbf{GMM --- Gaussian Mixture Model (EM)} : modèle probabiliste qui
    suppose que les données sont générées par un mélange de distributions
    gaussiennes. L'algorithme Expectation-Maximization (EM) estime les paramètres
    de chaque gaussienne.

    \item \textbf{DBSCAN --- Density-Based Spatial Clustering} : algorithme basé
    sur la densité. Il regroupe les points denses en clusters et marque comme
    bruit les points isolés. Il ne nécessite pas de spécifier $k$ à l'avance.
\end{itemize}
\section{Sélection automatique du nombre de clusters k}
K-Means, Agglomerative et GMM nécessitent de fixer $k$ avant l'exécution.
Pour choisir $k$ automatiquement, nous utilisons le \textbf{Silhouette Score},
calculé pour $k$ allant de 2 à 8, et nous retenons la valeur qui le maximise.

Le Silhouette Score mesure à quel point chaque point est bien placé dans son
cluster :

\begin{equation}
    s(i) = \frac{b(i) - a(i)}{\max(a(i),\ b(i))}
\end{equation}

où $a(i)$ est la distance moyenne au sein du cluster (cohésion) et $b(i)$ est
la distance au cluster voisin le plus proche (séparation). Un score proche
de 1 indique des clusters bien séparés.
Sur nos données (119 articles, \texttt{seed=42}),
le score est maximal pour $k = 4$ avec un Silhouette Score de 0,404.

\section{Métriques d'évaluation des clusterings}
Pour comparer les algorithmes de façon rigoureuse, nous utilisons trois métriques complémentaires :
\begin{table}[h]
\centering
\resizebox{\textwidth}{!}{%
\begin{tabular}{|p{3cm}|p{2cm}|p{9cm}|}
\hline
\rowcolor{gray!30}
\textbf{Métrique} & \textbf{Sens} & \textbf{Ce que ça mesure dans notre contexte} \\
\hline
Silhouette Score & $\uparrow$ mieux &
Mesure si chaque article est bien placé dans son cluster.
Un score élevé signifie que les articles d'un même batch
sont proches entre eux et éloignés des autres batches,
ce qui produit des tournées locales courtes. \\
\hline
Davies-Bouldin & $\downarrow$ mieux &
Mesure la compacité des clusters. Un index faible signifie
que les articles sont serrés à l'intérieur de leur batch
et que les batches sont bien éloignés les uns des autres. \\
\hline
Calinski-Harabasz & $\uparrow$ mieux &
Compare la dispersion entre les clusters et la dispersion
à l'intérieur des clusters. Un score élevé indique que
les batches sont bien distincts et denses géographiquement. \\
\hline
Temps d'exécution (ms) & $\downarrow$ mieux &
Durée du pré-traitement ML avant la métaheuristique.
Doit rester très court pour ne pas alourdir le pipeline. \\
\hline
\end{tabular}%
}
\caption{Métriques d'évaluation des algorithmes de clustering}
\label{tab:metriques}
\end{table}
\section{Résultats et comparaison}
Les expériences ont été menées sur un entrepôt de 200 × 200 unités, 119 articles, seed=42, k=4
, sous Python 3.13 / scikit-learn. Voici les résultats obtenus :
\begin{table}[h]
\centering
\resizebox{\textwidth}{!}{%
    \begin{tabular}{|l|c|c|c|c|c|c|}
\hline
\rowcolor{gray!40}
\textbf{Algorithme} & \textbf{k} & \textbf{Silhouette ↑} & \textbf{Davies-Bouldin ↓} & \textbf{Calinski-Harabasz ↑} & \textbf{Bruit} & \textbf{Temps (ms)} \\
\hline
\rowcolor{blue!15}
\textbf{K-Means} & \textbf{4} & \textcolor{green!60!black}{\textbf{0,404}} & \textbf{0,800} & \textcolor{green!60!black}{\textbf{115,33}} & 0 \% & 33,38
 \\
\hline
Agglomerative & 4 & 0,361 & \textcolor{green!60!black}{0,810} & 96,40 & 0 \% & \textcolor{red}{37,42} \\
\hline
GMM (EM) & 4 & \textcolor{red}{0,390} & \textcolor{red}{0,831} & \textcolor{red}{109,21} & 0 \% & 24,45\\
\hline
DBSCAN & 1 & \textcolor{gray}{N/A} & \textcolor{gray}{N/A} & \textcolor{gray}{N/A} & 0,8 \% & \textcolor{green!60!black}{7,97} \\
\hline
\end{tabular}%
}

\caption{Comparaison des quatre algorithmes de clustering (119 articles, k=4, seed=42).}
\label{tab:comparaison}
\end{table}

\begin{itemize}
\item \textbf{K-Means} obtient le meilleur Silhouette Score (0,404) et le meilleur Calinski-Harabasz (115,33), confirmant que ses clusters sont à la fois compacts et bien séparés géographiquement. Son temps d'exécution de 33,38 ms est raisonnable et son partitionnement est déterministe (grâce à la graine aléatoire fixée). C'est l'algorithme retenu pour notre pipeline.

\item \textbf{Agglomerative Clustering} présente le meilleur Davies-Bouldin (0,810), ce qui signifie que ses clusters sont plus compacts en termes de dispersion interne. Cependant, son temps d'exécution est légèrement plus élevé que K-Means (37,42 ms) pour un gain de Silhouette négligeable ($-$0,043). Dans notre contexte où le pré-traitement doit être rapide, ce rapport coût/bénéfice est défavorable.

\item \textbf{GMM (Gaussian Mixture Model)} donne les scores les plus faibles sur toutes les métriques de qualité (Silhouette 0,390, Davies-Bouldin 0,831, Calinski-Harabasz 109,21). Cela s'explique par le fait que le modèle gaussien n'est pas adapté à une distribution spatiale uniforme : les articles sont répartis aléatoirement dans l'entrepôt et ne suivent pas naturellement des distributions elliptiques gaussiennes.

\item \textbf{DBSCAN a échoué dans cette configuration :} il n'a trouvé qu'un seul cluster (k=1), regroupant tous les articles en un seul batch, ce qui rend son utilisation impossible pour notre pipeline. Ce résultat illustre une limitation connue de DBSCAN : sa sensibilité au paramètre eps. Avec une distribution uniforme des articles, la densité est homogène sur tout l'entrepôt, rendant difficile la détection de zones denses distinctes.
\end{itemize}
\section{Visualisations}
La figure 1 montre côte à côte les résultats des quatre algorithmes sur les mêmes données. Pour K-Means et Agglomerative et GMM  produisent des partitionnements visuellement similaires sur ces données. Les différences s'observent principalement dans les métriques quantitatives, notamment le Silhouette Score légèrement inférieur pour GMM (0,404, 0,361 , 0,390 respectivement). DBSCAN regroupe tout en un seul cluster (orange), ce qui confirme l'échec de l'algorithme.
\begin{figure}[H]
    \centering
    \includegraphics[width=0.98\linewidth]{../../ml/fig1_comparaison_clusterings.png}
    \caption{Comparaison des quatre clusterings sur 119 articles (entrepôt 200×200).}
    \label{fig:comparaison_clusterings}
\end{figure}
La figure 2 montre que K-Means domine sur les métriques de qualité (Silhouette et Calinski-Harabasz), tout en maintenant un temps d'exécution modéré. DBSCAN apparaît comme le plus rapide (7,97 ms), mais ses métriques de qualité sont toutes à N/A, ce qui le disqualifie.
\begin{figure}[H]
    \centering
    \includegraphics[width=1\linewidth]{../../ml/fig2_metriques_comparaison.png}
    \caption{Métriques comparatives par algorithme (Silhouette ↑, Davies-Bouldin ↓,
    Calinski-Harabasz ↑, Temps ↓).}
    \label{fig:metriques_comparaison}
\end{figure}
\section{Choix de ML - K-Means}
Au terme de la comparaison, K-Means est retenu pour cinq raisons :
\begin{itemize}
    \item Meilleure qualité de clustering (Silhouette 0,404 et Calinski-Harabasz 115,33) : les batches sont géographiquement cohérents.
    \item Résultats reproductibles grâce à la graine aléatoire fixée (seed=42), comme exigé dans le cahier des charges.
   \item\textbf{Intégration directe :} K-Means fournit nativement des labels et des centroïdes, exactement ce dont la métaheuristique a besoin.
   \item Pré-traitement rapide (33,38 ms), négligeable devant le temps de l'ACO/AG.
   \item \textbf {Cohérence avec la distance de Manhattan :} K-Means minimise la distance euclidienne, très proche de la distance de Manhattan dans un entrepôt à grille orthogonale.
\end{itemize}
\space La figure 3 présente en détail le clustering K-Means retenu. À gauche, la carte de l'entrepôt montre les 4 clusters avec leurs centroïdes et le nombre d'articles par cluster entre parenthèses. À droite, le diagramme en barres montre la distribution des tailles de clusters, avec la moyenne (environ 30 articles par batch) matérialisée par la ligne pointillée rouge.
On observe que les clusters sont globalement équilibrés (de 23 à 39 articles), ce qui est favorable pour la métaheuristique : des batches de taille similaire garantissent des sous-TSP comparables en complexité, évitant qu'un seul batch ne concentre l'essentiel de la distance totale parcourue. Le cluster C2 (39 articles) est légèrement sur-représenté, ce qui pourrait être optimisé en ajustant k ou en ajoutant une contrainte de taille dans le clustering.
\begin{figure}[H]
    \centering
    \includegraphics[width=1\linewidth]{../../ml/fig3_best_clustering_detail.png}
    \caption{K-Means (k=4) : carte de l'entrepôt avec centroïdes et distribution des tailles de clusters.}
    \label{fig:best_clustering}
\end{figure}
\section{Intégration avec la métaheuristique}
Le module ML transmet à la métaheuristique les batches (articles par cluster), les centroïdes, la matrice de distances de Manhattan entre centroïdes, et les coordonnées du dépôt. La métaheuristique n'a donc plus à traiter 119 points d'un seul coup, mais
1 TSP de 4 nœuds + 4 sous-TSP d'environ 30 articles chacun.

\textbf{Gain de complexité :} Sans clustering : 119! permutations. Avec clustering : 4! + 4 × 30! réduction drastique de l'espace de recherche.

\chapter{Métaheuristiques et Optimisation}

Dans la continuité du pré-traitement par clustering K-Means, qui a permis de diviser notre problème initial de 119 articles en 4 sous-TSP de taille plus réduite, nous appliquons des métaheuristiques afin de déterminer la tournée optimale du préparateur au sein de chaque lot.

\section{Implémentation des Algorithmes}

\subsection{Algorithme des Colonies de Fourmis (ACO)}

L’ACO est une métaheuristique bio-inspirée particulièrement adaptée au problème du Voyageur de Commerce (TSP). Dans notre implémentation, chaque "fourmi" artificielle construit une solution probabiliste en tenant compte de deux facteurs pour se déplacer d’un point $i$ vers un article $j$ :

\begin{itemize}
    \item \textbf{La visibilité ($\eta_{ij}$)} : l’inverse de la distance de Manhattan entre les deux points.
    \item \textbf{La trace de phéromone ($\tau_{ij}$)} : l’historique des meilleurs chemins trouvés.
\end{itemize}

La probabilité de transition est donnée par :

$$
P_{ij} = \frac{(\tau_{ij})^\alpha \cdot (\eta_{ij})^\beta}{\sum_{k} (\tau_{ik})^\alpha \cdot (\eta_{ik})^\beta}
$$

Ce mécanisme permet une convergence progressive vers des solutions de qualité.

\subsection{Algorithme Génétique (AG)}

L’algorithme génétique repose sur une population de solutions (chromosomes).
Afin de respecter les contraintes du TSP, nous avons utilisé l’opérateur \textbf{Order Crossover (OX)}, garantissant l’absence de doublons.
Une mutation par inversion est également introduite afin de maintenir la diversité et éviter les minima locaux.

\section{Résultats et Comparaison des Approches}

Nous avons comparé quatre scénarios :

\begin{enumerate}
    \item \textbf{ACO sans ML}
    \item \textbf{AG sans ML}
    \item \textbf{ACO avec ML (K-Means, k=4)}
    \item \textbf{AG avec ML (K-Means, k=4)}
\end{enumerate}

\subsection{Tableau de synthèse}

\begin{table}[H]
\centering
\begin{tabular}{|l|c|c|}
\hline
Approche & Distance & Temps (s) \\
\hline
ACO sans ML & 2588 & 3.32 \\
AG sans ML & 12052 & 5.37 \\
ACO avec ML & 3334 & 1.34 \\
AG avec ML & 5600 & 4.42 \\
\hline
\end{tabular}
\caption{Comparaison des performances}
\end{table}

\subsection{Analyse des performances}

L’ACO sans Machine Learning produit la meilleure distance (2588), car il optimise une tournée globale. Cependant, son temps de calcul reste élevé.

L’algorithme génétique sans Machine Learning est fortement pénalisé par l’explosion combinatoire, ce qui entraîne une distance très élevée et un temps important.

L’introduction du clustering (K-Means) permet de réduire significativement le temps de calcul. L’ACO avec ML divise le temps par plus de deux (1.34 s), au prix d’une légère augmentation de la distance (3334), due aux retours au dépôt.

L’AG avec ML améliore ses performances par rapport à sa version sans ML, mais reste moins efficace que l’ACO.

\subsection{Visualisation des performances}

Afin de mieux interpréter ces résultats, nous avons représenté graphiquement les performances des différentes approches.

\paragraph{Comparaison globale}

La figure suivante montre simultanément la distance et le temps :

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{../../fig_barres_performances.png}
\caption{Comparaison des distances et temps de calcul}
\end{figure}

On observe que :
\begin{itemize}
    \item l’ACO sans ML minimise la distance,
    \item l’ACO avec ML réduit fortement le temps,
    \item l’AG sans ML est le moins performant,
    \item l’AG avec ML améliore ses résultats mais reste inférieur à l’ACO.
\end{itemize}

\paragraph{Compromis Temps / Distance}

La figure suivante permet d’analyser le compromis :

\begin{figure}[H]
\centering
\includegraphics[width=0.75\textwidth]{../../fig_scatter_pareto.png}
\caption{Compromis temps / distance}
\end{figure}

Dans ce plan, la zone idéale se situe en bas à gauche.

On observe que :
\begin{itemize}
    \item l’ACO avec ML est le meilleur compromis,
    \item l’ACO sans ML est très performant mais plus lent,
    \item l’AG reste globalement moins efficace.
\end{itemize}

\section{Interprétation}

L’augmentation de la distance dans les approches hybrides ne constitue pas une faiblesse, mais reflète une modélisation plus réaliste des contraintes logistiques (retours au dépôt).

L’ACO s’impose comme la métaheuristique la plus adaptée au problème de routage, grâce à son mécanisme d’apprentissage progressif.

À l’inverse, l’algorithme génétique nécessite un réglage plus fin pour atteindre des performances comparables.


\chapter{Conclusion}

Ce projet a démontré que l'hybridation entre le Machine Learning
et les métaheuristiques constitue une approche efficace pour
résoudre des problèmes d'optimisation combinatoire complexes.

L'apport principal du Machine Learning ne se limite pas à améliorer
les résultats : il transforme fondamentalement la nature du problème
en le rendant traitable. Face à la complexité combinatoire, la clé
n'est pas toujours de chercher un algorithme plus puissant, mais
de poser le problème différemment.

L'approche hybride ACO + K-Means apparaît comme la solution la plus
pertinente, offrant un excellent compromis entre rapidité, qualité
de solution et réalisme opérationnel. Cette architecture pourrait
être étendue à des contextes plus complexes, tels que des entrepôts
avec contraintes de capacité ou des commandes dynamiques en temps réel.


\chapter{Ressources et code source}
L'intégralité du code source de ce projet est disponible sur GitHub :
\begin{center}
    \url{https://github.com/kenza-menad/Optimisation-combinatoire}
\end{center}
\end{document}